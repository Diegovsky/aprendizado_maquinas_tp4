# Shell
User uses the Fish shell, which doesn't support HEREDOC. Just edit the file directly.

Avoid using complicated shell commands with for,while, etc. Use python instead.

# Editability
User doesn't want base64-encoded commands.